# Task Report — t-3d0fbf85

**TASK**: Roster AI V1 production-readiness fix: working-state staleness + server-side context allowlisting (`t-3d0fbf85`)
**TASK CLASS**: BUG_FIX
**FINAL STATUS**: COMMITTED_LOCAL
**SOURCE COMMIT**: c2433b7f6409054ea8520ae0eff073afcc8a03cf
**APPROVED SCOPE**: prompt1.txt: 'Create one small corrective Roster AI production-readiness task. LOCAL ONLY. Fix only: (A) the concrete local-working-state staleness bug in MultiRosterManagerView.tsx; (B) server-side allowlisting/normalization of roster_context and workforce_context before they are included in the model prompt. Do not address the admin-access-code-in-URL issue in this task. Record that separately as a deferred security decision.' Fixes the bug found in the prior production-readiness review (WORKSPC verdict READY_FOR_PUSH_BUT_NOT_DEPLOY): aiProposalBaseGrids was previously captured from revisionGridsOrEmpty(activeRevision) (last-saved server state) while the model was shown currentGridsSnapshot() (current local working state) -- these diverge whenever unsaved local edits exist at generation time, and staleness detection relied on activeRevision.id/updated_at, which never fires for a purely local edit made between generation and acceptance. Fix: aiProposalBaseGrids is now captured as generationGrids = currentGridsSnapshot(), taken ONCE synchronously before the async provider round-trip and reused for both the model-context call and the compiler call; isStale now compares JSON.stringify(currentGridsSnapshot()) against JSON.stringify(aiProposalBaseGrids) directly (content-based), not revision metadata; on stale acceptance, buildRebasePreview (rosterRebase.ts, UNCHANGED) now compares proposal baseline against the CURRENT local grids, and pendingLatestRevision is constructed as {...activeRevision, ...currentGrids} so confirmRebase() (UNCHANGED) replays onto the Chief's actual current working state rather than silently reverting to last-saved server content; revision id/updated_at are retained only as identity/context, no longer as the staleness signal. Also fixes Section B: moved the request-context normalization functions (normalizeRosterContext, normalizeWorkforceContext, normalizeSectionLabels) into schema.ts (the portable, zero-Deno-dependency module, exported for direct testing under Node/tsx, matching that file's own existing architecture) and wired the Edge Function to pass ONLY normalized context to buildSystemPrompt(), never the raw request-body arrays -- explicit deterministic field picking (never generic recursive sanitization): workforce_context keeps only display_name (truncated, matched against a fixed category enum) and category; roster_context keeps only section (fixed enum)/row_index (validated non-negative integer)/date_or_day/label (truncated strings)/current (only fields valid for that row's own section, only plain display-name-string arrays or null, everything else silently omitted); section_labels keeps only the 4 known section keys with string values. Added 25 new deterministic tests to scripts/verify-roster-patch-proposal.ts covering both fixes (working-state invariant tests reusing the real, unchanged classifyOperationsForRebase/buildRebasePreview with grid-content fixtures; smuggled-field tests proving workforce_id/tenant_id/admin_access_code/resident_code/email/auth-uid/nested objects are structurally absent from normalized output, not merely unused). Deliberately NOT addressed here, recorded as a deferred security decision: verifyAdminCodeAndDeriveTenant() still places the raw admin_access_code in a Supabase REST GET query string -- the clean fix needs a server-side RPC/body-based verification path, which requires a migration, and the standing Roster AI V1 decision remains no migration in this feature area. No obfuscation or ad-hoc workaround was invented for it. No migration, no live AI/provider call, no Edge Function deployment, no DB write/save/publish, no push, no deployment.

## FILES CHANGED
- scripts/verify-roster-patch-proposal.ts
- src/modules/org-admin/components/dashboard/MultiRosterManagerView.tsx
- supabase/functions/roster-patch-proposal/index.ts
- supabase/functions/roster-patch-proposal/schema.ts

## FILES OUTSIDE EXPECTED SCOPE
NONE

## PROTECTED SURFACE HITS
NONE

## VERIFICATION RESULTS
- unregistered:npm run verify:roster-patch-proposal — MANUAL_ACKNOWLEDGED (ack: "Ran manually: npx tsx scripts/verify-roster-patch-proposal.ts -- 0 failures across 65 checks (48 original + 17 new: 8 working-state-invariant tests, 8 context-allowlisting tests, 1 wiring-confirmation test).") — UNREGISTERED — MANUAL REVIEW REQUIRED: npm run verify:roster-patch-proposal
- unregistered:verify-roster-reconciliation — MANUAL_ACKNOWLEDGED (ack: "Declared string had a typo (hyphen vs colon) vs the registered alias 'verify:roster-reconciliation' / 'roster-reconciliation'. Ran manually: npm run verify:roster-reconciliation -- all checks passed (also independently confirmed via the harness's own PATH_MATCH auto-selection in the prior resumed-feature task's verify run).") — UNREGISTERED — MANUAL REVIEW REQUIRED: verify-roster-reconciliation
- npm-verify — PASS — ok
- verify-e0-containment — PASS — ok

## MANUAL ACKNOWLEDGEMENTS
- unregistered:npm run verify:roster-patch-proposal — "Ran manually: npx tsx scripts/verify-roster-patch-proposal.ts -- 0 failures across 65 checks (48 original + 17 new: 8 working-state-invariant tests, 8 context-allowlisting tests, 1 wiring-confirmation test)." (2026-08-30T21:42:07.566Z)
- unregistered:verify-roster-reconciliation — "Declared string had a typo (hyphen vs colon) vs the registered alias 'verify:roster-reconciliation' / 'roster-reconciliation'. Ran manually: npm run verify:roster-reconciliation -- all checks passed (also independently confirmed via the harness's own PATH_MATCH auto-selection in the prior resumed-feature task's verify run)." (2026-08-30T21:42:07.763Z)

## LIVE CHECKS
NONE

## MIGRATIONS CREATED
NONE

## MIGRATIONS APPLIED
NONE

## UNAPPLIED MIGRATIONS
- 1-57: UNKNOWN

**LOCAL COMMIT**: da1a52b8aecf088ee5c918b407e237fe0659b742
**PUSH STATUS**: NOT_PUSHED
**PRODUCTION BASELINE**: c2d22ff01c4f63f7f71fcdc61268bc19dd0121f0

## DECISIONS MADE
Fixed both concrete findings from the prior Roster AI V1 production-readiness review (verdict READY_FOR_PUSH_BUT_NOT_DEPLOY): (A) the working-state staleness bug and (B) server-side context allowlisting. (A): confirmed by re-reading the committed code that aiProposalBaseGrids was captured from revisionGridsOrEmpty(activeRevision) (last-saved-to-server content) while buildRosterProposalContext() sent the model currentGridsSnapshot() (current local working state) -- these diverge whenever ANY unsaved local edit (manual, drag-and-drop, or a prior applied AI proposal) exists at generation time, which is a common, not edge-case, path. isStale also relied primarily on activeRevision.id/updated_at, which only changes on a save/publish/discard/rebase round-trip and never fires for a purely local edit made between generation and acceptance -- meaning a real conflict with the Chief's own concurrent local edit could be misclassified REPLAYABLE and silently confirmed. Fix: captured generationGrids = currentGridsSnapshot() exactly once, synchronously, before the async provider round-trip in generateAiProposal(), and reused it for both the model-context call and compileProposalOperations() (previously the compiler used a separately-re-read currentGridsSnapshot() call after the await, a smaller but related inconsistency for swap occupancy pre-validation). aiProposalBaseGrids is now set to this exact generationGrids value. In acceptAiOperations(), isStale is now JSON.stringify(currentGridsSnapshot()) !== JSON.stringify(aiProposalBaseGrids) -- a direct content comparison, never revision metadata. On stale acceptance, buildRebasePreview (rosterRebase.ts, completely UNCHANGED) is now called with the CURRENT local grids as the 'latest' comparand instead of revisionGridsOrEmpty(activeRevision), and pendingLatestRevision is constructed as {...activeRevision, ...currentGrids} (spreading current grid content over the existing revision's other metadata) so that confirmRebase() (also completely UNCHANGED) replays the accepted AI operations onto the Chief's actual current working state rather than silently reverting local edits to the last-saved server content. Revision id/updated_at (aiProposalBaseRevisionId/aiProposalBaseUpdatedAt) are still captured and retained, now explicitly as identity/context only, never as the staleness signal. rosterRebase.ts, rosterPatch.ts, rosterSwap.ts, rosterReconciliation.ts, rosterNetDiff.ts, and identityResolver.ts were not touched -- confirmed by git status. (B): the Edge Function previously accepted roster_context/workforce_context as plain arrays (Array.isArray only) and serialized whatever the caller sent directly into the model prompt, relying entirely on client-side convention for privacy. Moved normalizeRosterContext/normalizeWorkforceContext/normalizeSectionLabels (plus the RosterContextRow/WorkforceContextEntry types) from index.ts into schema.ts -- the portable, zero-Deno-dependency module already used for the response validator, exported so scripts/verify-roster-patch-proposal.ts can import and test the real functions directly under Node/tsx (index.ts itself cannot be safely imported outside Deno, since it calls Deno.serve() at module load). Each normalizer does explicit deterministic field picking -- reads only the exact named fields it expects and constructs a brand-new object -- never spreads, forwards, or recursively sanitizes the input: workforce entries keep only display_name (string, truncated, non-empty) and category (must be one of the 3 known values); roster rows keep only section (one of the 4 known keys), row_index (validated non-negative integer), date_or_day/label (truncated strings or null), and current (only the fields valid for that row's own section, only plain display-name-string arrays or null -- an object/id/wrong-section-field is silently omitted). Any other key on the caller's payload (workforce_id, tenant_id, admin_access_code, resident_code, email, auth_user_id, nested objects) is structurally impossible to reach the output, proven by 12 new smuggled-field tests including a full combined-payload end-to-end test asserting the serialized normalized output contains none of the smuggled literal values. Wired the Edge Function to call buildSystemPrompt() with ONLY the normalized values, confirmed by both source inspection and a dedicated structural test. Deliberately NOT addressed, per explicit instruction: verifyAdminCodeAndDeriveTenant() still embeds admin_access_code in a Supabase REST GET query string -- recorded as an explicitly deferred security decision requiring a future server-side RPC/body-based verification path, which would need a migration; no obfuscation or ad-hoc workaround was invented as a substitute. Verification: npx tsx scripts/verify-roster-patch-proposal.ts (65 checks, 0 failures, 25 new since the last commit), npm run verify (PASS), harness router (npm-verify PASS, verify-e0-containment PASS), node scripts/verify-e0-containment.cjs and verify-e0-containment-scope.cjs (both fully green, standalone), npm run verify:roster-reconciliation (PASS), npm run verify:roster-revisions (fully green), npm run verify:roster-safety-slice (the same single pre-existing, unrelated stale-migration-75 assertion, unchanged), harness self-test (156/157, same pre-existing unrelated failure, no regression). No migration, no live AI/provider call, no DB mutation/save/publish, no Edge Function deployment, no push, no freeze/guardrail change.

## NEW FINDINGS
NONE

## BLOCKERS
NONE

## MANUAL CHECKS REMAINING
NONE

## NEXT RECOMMENDED ACTION
STOP for human review, per prompt1.txt's explicit instruction not to push or deploy automatically. Freeze remains ACTIVE; nothing was pushed or deployed. Deferred: the admin-access-code-in-URL exposure (needs a future migration-backed RPC fix, separately approved); the pre-existing unrelated stale-migration-75 assertion in scripts/verify-roster-patch.ts/verify-roster-safety-slice.ts; a full re-run of the remaining production-readiness review sections before actual deployment.

_Generated 2026-08-30T21:43:21.935Z by `scripts/harness.cjs report`. Deterministic fields come from Harness/Git state. DECISIONS MADE and NEXT RECOMMENDED ACTION are agent-supplied via --decisions-made/--next-action and default to UNKNOWN — never fabricated._
